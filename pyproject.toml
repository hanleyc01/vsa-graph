[project]
name = "vsa-graph"
version = "0.1.0"
description = "VSA compiler tools"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "lark>=1.2.2",
    "numpy>=2.2.4",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
    "mypy>=1.15.0",
]
